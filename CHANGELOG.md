# @aliou/pi-xcode

## 0.0.2

### Patch Changes

- fd5ea54: Replace emoji characters in tool output with plain text alternatives (`[OK]`, `[FAIL]`, `[warn]`). Internal tooling updates: align `package.json` and `tsconfig.json` with extension template.
- 40b38b3: mark pi SDK peer deps as optional to prevent koffi OOM in Gondolin VMs
