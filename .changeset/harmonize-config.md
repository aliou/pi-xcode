---
"@aliou/pi-xcode": patch
---

Replace emoji characters in tool output with plain text alternatives (`[OK]`, `[FAIL]`, `[warn]`). Internal tooling updates: align `package.json` and `tsconfig.json` with extension template.
